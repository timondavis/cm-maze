/**
 * Intake maze data and generate the corresponding class
 */
import {Maze} from "cm-maze";

export class MazeLoader {

    public static mazeFactory(data: any) : Maze {
        const m = new Maze();

        //m.setCardinality(new )

        return m;
    }
}
