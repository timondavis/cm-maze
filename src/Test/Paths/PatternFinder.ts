import {MazeBuilder} from "../../MazeBuilder";
import {PathFinder} from "../..";

describe ('PatternFinder', () => {

	it ('supplies all tiles within range of a given node within a given maze', () => {
		let MB = new MazeBuilder();
		let maze = MB.buildMaze();


	});
});