import {MazeBuilder} from "../MazeBuilder";

describe( 'Maze', () => {

    it( 'works', () => {

        let MB: MazeBuilder = new MazeBuilder();
        let maze = MB.buildMaze();

        let a = 1;
    });
});